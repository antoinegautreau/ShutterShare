package ca.unb.mobiledev.shuttershare.entity

// startTime and endTime can maybe change to some date thing if need be
class Event (val code: String? = null, val name: String? = null, val endTimestamp: Long = 0) {
}